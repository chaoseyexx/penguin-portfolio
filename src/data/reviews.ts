export interface Review {
    id: string
    name: string
    project: string
    avatarColor: string
    rating: number
    content: string
}

export const reviews: Review[] = [
    // No reviews currently available
]
